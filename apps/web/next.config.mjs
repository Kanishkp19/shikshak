/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Tailwind v4 — no tailwind.config.js needed; configuration lives in globals.css
  },
};

export default nextConfig;
